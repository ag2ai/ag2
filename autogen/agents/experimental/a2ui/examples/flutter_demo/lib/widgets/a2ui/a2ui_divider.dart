import 'package:flutter/material.dart';

class A2UIDivider extends StatelessWidget {
  const A2UIDivider({super.key});

  @override
  Widget build(BuildContext context) {
    return const Divider(color: Color(0xFF334155), height: 16);
  }
}
